postgres {
  root {
    username = "postgres"
    password = "secret"
    database = "postgres"
  }
  app {
    username = "dukkhaless"
    password = "secret"
    database = "dukkhaless"
  }
  host = "localhost"
  port = 5432
}

crypto {
  signingKey = "tacos"
}

http {
  domain = "localhost:4000"
}